/**
 * 从数据库中抓取数据
 */
/*
const fs=reauire('fs');
var file="d:\\0.json";
const gasEqus = fs.readFileSync('/gisdatas/oradata_exp_gasep.json');
var result=JSON.parse(fs.readFileSync( file));

const redis = require("redis"),
  client = redis.createClient({
    host: '10.5.4.145',
    port: 16379,
    db: 8
  });
*/

module.exports = {



};
